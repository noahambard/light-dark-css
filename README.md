# light-dark-css

A robust proof-of-concept on system colour scheme matching in CSS merged with a JavaScript-enhanced manual colour scheme dropdown.

Includes a custom `<select>` styling with an icon.
